---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: menugroups.component.t7d.io
spec:
  group: component.t7d.io
  names:
    kind: MenuGroup
    listKind: MenuGroupList
    plural: menugroups
    singular: menugroup
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: MenuGroup is the Schema for the menugroups API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MenuGroupSpec defines the Position of the menu
            properties:
              column:
                description: menu column position
                type: integer
              name:
                description: Menu name
                type: string
              ranking:
                description: ranking in Column, the smaller the number, the higher
                  the position 菜单组在当前列中的排序，数字越小越靠前
                type: integer
              type:
                type: string
            required:
            - column
            - name
            - ranking
            - type
            type: object
          status:
            description: MenuGroupStatus defines the observed state of MenuGroup
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
